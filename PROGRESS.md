# PROGRESS

## Weak list
absolute vs relative paths | chmod and permissions | redirection > and >> | pipes and grep (watch for unnecessary `cat`) | man and --help | mkdir -p | chown/ownership vs authorship | apt update vs upgrade | git diff (staging-as-snapshot) | tenant->subscription->RG hierarchy | git branch/merge/conflict resolution (taught 08-28, one pass, needs confirm) | sed substitute syntax (taught 08-28, took 2 tries to write cold) | cut/sort/uniq pipeline (understands mechanism, flagged own difficulty reproducing from memory — needs repeated real use, not re-explanation)

## Setup
- [x] Repo moved to ~/azure-static-site
- [x] Azure budget alert set (self-reported, not CLI-verified)
- [x] Repo permissions fixed (777 -> dirs 755, files 644, deploy.sh 750)

## Log
2026-08-18 | Setup. Diagnostic 8/22. No reps yet.

2026-08-21 | Recall drill on full weak list (paths, ~/, chmod, >/>>, pipes+grep, man/--help, mkdir -p): 0/7 cold, 7/7 after teaching+hands-on. Still weak until demonstrated cold — re-test without warning next session. Real task: fixed 777 perms across repo (find+chmod), found and fixed CRLF line-ending bug in deploy.sh (git diff showed every line changed — taught CRLF vs LF, why it breaks shebang lines). Learned git only tracks executable-bit as file mode (100644/100755), not full rwx; this repo has core.fileMode=false so chmod changes aren't tracked at all. Cost hygiene: budget alert set, deleted empty rg-learning resource group. Pushback logged: user flagged the git-internals tangent (core.fileMode, racy git stat-cache) as too deep/not retainable — agreed, not added to weak list, scope trimmed back to chmod/CRLF fundamentals.

2026-08-21b | 40-min block: new concept, chown + sudo. Hands-on: chown to root, tried to reclaim as bunny (skipped a step first attempt, corrected), confirmed owner flip in both directions. Had to reset forgotten Linux/sudo password via `wsl -u root` / `passwd bunny` — worth a quick re-check next session that they still remember how sudo auth works. Verbal check on "why does reclaiming your own file need sudo" was vague first pass ("for security") — corrected to precise mechanism (ownership, not authorship, is what matters). Add **chown/ownership vs authorship** to weak list, not yet demonstrated cold.

2026-08-24 | Recall round 2, cold, no notes, 8 items (7 Linux + chown/ownership): #1 paths, #4 redirection, #7 mkdir -p correct cold. #2 (`~` vs pwd) and #8 (ownership vs authorship) FAILED COLD AGAIN — same two mistakes as last session, both eventually corrected after re-teaching + hands-on proof, but two failures in a row means these are not solid, needs another unannounced retest before trusting them. #3 (chmod math) and #5 (pipe direction, had it backwards on one variant) — minor errors, fixed same round. #6 (man vs --help) went fully blank despite being demonstrated hands-on last session — pure retention failure, fixed with a mnemonic ("--help = cheat sheet, man = the whole book"), needs retest. Net: 8/8 after corrections, but only 3/8 clean cold. Retention across sessions is the actual weak point, not first-pass understanding.

2026-08-24b | Full day continued. New concepts: env vars & PATH (plain var vs export, verified via `env`, `$` substitution mistakes corrected, `VAR = "x"` spacing bug corrected), process management (`ps aux`, `jobs`, `kill %1` vs `kill PID`, background `&`) — clean hands-on reps, both locked after one redo each. Project work: read and explained deploy.sh line-by-line before running (initially gave a wrong/unrelated answer, confusing it with an old "hello" script — redone properly). Ran deploy.sh for real: resource group created fine, storage account create failed with misleading `SubscriptionNotFound` — root cause was `Microsoft.Storage` resource provider not registered on the subscription (fixed via `az provider register --namespace Microsoft.Storage`, real AZ-104-relevant gotcha). Full deploy succeeded second time — live static site verified in browser, then torn down cleanly (`az group delete`, confirmed empty via `az group list`). Noted for later (not yet taught): `az storage` commands fell back to account-key auth with a warning — revisit with `--auth-mode login`/RBAC when we hit Identity. Recurring behavior flag: pasted stale/duplicate terminal output as "proof" three separate times this session (old git status, old MYVAR block, "same again" with no output) — kept getting caught and re-asked; keep enforcing the paste-real-output rule hard, it's not sticking yet.

2026-08-24c | Redo-old-tasks block (chmod, grep, redirection), reworded not verbatim repeats. #1 chmod for "owner rw, group r, others none" — first answer 740 (gave owner unneeded execute bit, least-privilege miss), corrected to 640. #2 grep without pipe — answer included the pipe version anyway despite explicit "no pipe" constraint; correct command was in there but didn't follow the instruction as given, same "useless use of cat" reflex as session 1. #3 (new: why `>` breaks a recurring/cron logging job) — took three passes: first "idk," then described `>>`'s fix instead of `>`'s failure, then landed on it correctly (repeated runs only ever keep the last run's data, full history lost). Watch for this pattern generally: tends to answer an adjacent/easier question instead of the one asked, needs redirecting back each time.

2026-08-25 | AZ-900 vocab block (30 min line item, first time actually done — was 0 min across prior sessions). Tenant/subscription/RG hierarchy — this is the weak item flagged since diagnostic day 1 — first pass was wrong on all three (tenant="a user", RG="files"), corrected, then redone: second attempt was fully correct and complete (tenant=identity boundary in Entra ID, subscription=billing+access boundary, multiple per tenant, RG=lifecycle container for resources, multiple per subscription, full chain stated right). Best result on this item yet — still needs one unannounced retest before calling it solid, given the pattern this session of things taking 2-3 passes. ARM corrected (confused it with Cost Management — ARM is the control-plane API every tool goes through, not a cost dashboard) and regional-vs-global corrected (storage account = regional, Entra ID/tenant = global) — neither retested yet, check cold next session. End-of-day status: Linux ~1/3 of depth target covered, Git still untaught formally (only used operationally), Azure CLI has one real end-to-end deploy rep, AZ-900 vocab started. Retention (not pace) flagged directly to user as the actual risk to the 3-month timeline — repeat cold-recall failures on already-taught material across sessions, tendency to answer an adjacent easier question instead of the one asked, and claims-without-output happened 3x this session alone.

2026-08-25b | New session, opened with date/time/hours-available format going forward (11:30am, ~7hrs). Retest of 6 items from last session's weak spots: `~` vs pwd FAILED COLD AGAIN (4th session in a row on this exact item — verbalization problem specifically, hands-on proof has worked twice, cold recall keeps failing; needed 2 redo passes this time before landing), ownership/sudo correct first try, man vs --help wrong first try (drifted into a wrong mental model — confused it with reviewing file changes, likely cross-wired with git diff — corrected, needed 2 passes), tenant/sub/RG correct first try (holding steady), ARM wrong again (conflated with Cost Management/billing — corrected, now excludes billing correctly), regional vs global — right examples (VM regional, Entra ID/Traffic Manager global) but couldn't explain why until prompted twice. Moved into first formal Git lesson: remotes/origin (clean, confirmed SSH remote, correctly explained "up to date" after one correction) and git diff staged-vs-unstaged (`--cached`). Mid-drill, user skipped two script steps then ran a scripted `git reset --hard HEAD~1` cleanup line I had given as part of a fixed command block without checking state first — since the intended test commit never actually happened (git correctly refused an empty commit), the reset instead rolled back the real last commit ("Add CLAUDE.md and PROGRESS.md") and wiped uncommitted PROGRESS.md session-log edits. Fully recoverable: nothing had been pushed, so `origin/main` still had the real commit; `git pull` restored it. Uncommitted log edits reconstructed from conversation context (this file). **Process fix, applies to me too**: never hand over `git reset --hard` (or any discard-type command) as a rote step in a script — always check `git status`/`git log` immediately before running it. Real teaching moment on why frequent small commits matter: work that's never committed is exactly this fragile. Recovery committed and pushed clean (`19730cf`). Re-ran git diff / staging lesson safely afterward (no destructive commands this time): staged vs unstaged vs last-commit fully demonstrated, `git diff` blank after `git add` / `git diff --cached` showing the staged-vs-HEAD difference — clean rep, though the "why" explanation was circular on first pass, tightened after one correction. Cleaned up test change with `git restore --staged` + `git restore` (working tree clean, confirmed). Git remotes/origin + git diff both now formally taught, no longer just used operationally — can come off "never taught" status, still need a cold unannounced retest before calling them solid.

2026-08-25c | Final hour: apt package management, first time taught. Installed `tree` for real (`sudo apt update` → `install -y tree`), confirmed with `tree --version` and `tree ~/azure-static-site` showing the actual repo structure correctly. `update` vs `upgrade` distinction wrong on first pass ("upgrade = best available product" — vague), corrected to precise mechanism (update = refresh local catalog only, no install; upgrade = actually install newer versions using that catalog) and restated correctly second try. Add **apt update vs upgrade** to weak list, one correct restatement only, needs cold retest before trusting it.
2026-08-27 | 4hr day. Recall done scenario-style for the first time (per explicit user feedback from last session — see request to make quizzes tougher/application-based). Results: `~` vs `pwd` — first pass wrong again (still said "owner doesn't change unless sudo," missed that a different user account gets a different home dir entirely, exposing that my own earlier teaching of "~ always equals /home/bunny" was an oversimplification) — corrected to the general rule (~ = current user's home dir, resolved per-user, not a fixed string) and restated correctly. `git origin` — first pass wrong ("goes to whichever remote was added last"), corrected to the real mechanism (push follows the branch's tracked remote, set at clone time to `origin`, unaffected by adding more remotes) and restated correctly — this is `origin`'s second consecutive cold pass (also passed cold in the 08-26 closing quiz), reasonable confidence now. `apt` cause/fix scenario — correct reasoning, sloppy terminology (mixed up `upgrade` and `install`), corrected. `git diff` after staging+delete — correct on first try, good instinct on staging-as-snapshot. Then closed out all three items that have sat untouched since the day-1 diagnostic: **static vs dynamic sites** (correctly reasoned using their own deploy.sh as the reference point — static can't personalize per-user without server-side logic), **ports** (correctly identified NSG as the blocking layer in a port-80-blocked scenario), **Entra ID/access tokens** (correct on second pass — named the actual mechanism: one login issues an access token, that's what's checked per-request, not the password again, with silent refresh before ~1hr expiry). All three were preview-level (full Identity/Networking depth still ahead), taught and passed same-day — flagged for one confirm-cold-pass next session before fully retiring, per the standard here. Weak-list header trimmed: removed `~ vs /` and `git remotes and origin` after two consecutive clean cold passes across separate days each.

2026-08-28 | 5hr day. Recall confirmed all three items pending from yesterday cold on a fresh day (static/dynamic, ports, Entra ID/tokens all passed) — retired from weak list for good. Also re-confirmed `apt` cause/fix and `git diff` staging scenarios, both solid. Major new-concept block: git branches, merge (fast-forward), and a real merge conflict set up and manually resolved (edited out conflict markers in nano, staged, committed a genuine merge commit with two parents). Also covered why/how to minimize conflicts in practice (short-lived branches, frequent merges, communication). **Notable meta-moment**: user explicitly asked how to build the skill of spotting mistakes from terminal output alone, not just following instructions — answered directly: it comes from knowing what success output looks like ahead of time and comparing against it, which is exactly why predictions-before-running have been required throughout; reinforced they should keep doing this deliberately, not skip it. Text processing block: `wc`, `sort`, `uniq`, `cut` taught together via a real log-analysis pipeline (`cut | sort | uniq -c | sort -rn` — find most frequent IP), executed cleanly with a correct prediction. User asked good "why" questions again (why does uniq need adjacency, why can't cut do uniq's job) — answered with real Unix-philosophy reasoning (small composable tools) rather than just re-describing syntax; explicitly told them not remembering pipeline syntax cold is normal and needs repeated real use, not memorization — flagged as a spaced-repetition target rather than a gap. Closed with `sed` substitute syntax, tied to a command they'd already run weeks ago without understanding (`sed -i 's/\r$//' deploy.sh`) — first attempt at a new substitution had 3 separate syntax errors (missing `s`, reversed find/replace order, missing `g` flag), corrected and restated cleanly on second try.

2026-08-29 | 2hr day. Quick recall: chmod+x vs sudo distinction solid, `sed` structure recalled correctly on retry after initially trying to opt out citing "needs practice" — pushed back since that excuse applies to complex multi-tool pipelines, not a single fixed-structure command already written correctly twice; worth remembering not to let that phrase become a blanket avoidance move. Started real shell scripting (`for` loop + `if [ -x ]` test using the actual chmod+x knowledge from earlier). Ran short on time before finishing.
2026-08-30 | 30min day (dropped to 15 after user cited a daily-GitHub-push habit and no time to write code). Scaled the task down rather than to zero — for-loop-only version of the script, skipping the if/else for now, real content not just a ritual commit. Hit real friction executing it: nano line-delete confusion, then got stuck mid-heredoc needing Ctrl+C to escape — switched to single-line `printf` (a method already proven to work for them) to get unblocked fast. `check_scripts.sh` (for loop over `*.sh`, printing each match) done, tested (correctly found both itself and deploy.sh), committed, pushed. `if`/`else` block still owed on top of this file — pick up there next real session.

2026-08-30b | Squeezed in the owed `if`/`else` block in the last ~10 min via the same reliable `printf` method — script now correctly reports executable status for both `.sh` files in the repo, verified with real pasted output, committed and pushed (`06f86e1`). User wants tomorrow to be a full, unhurried redo of everything from scratch — treat that as the plan, not a new topic. Given two sessions in a row this week were heavily time-constrained (30min, then 10min), don't assume the same tomorrow — confirm actual available time before deciding scope.

2026-08-31/09-02 | Full unhurried redo across 4 sections (spanned across a couple real days but treated as one continuous review): **Linux fundamentals** — chmod math solid, `chown root` ownership scenario was a real regression (reverted to "it's my file" reasoning that had already been corrected twice before — third time now, watch this one closely, may need to stay on weak list longer than usual), apt update/upgrade solid, man-vs-`--help` needed the "unfamiliar command → man" distinction re-drawn. **Git** — `origin` correct after dropping the "tied to main" framing, fast-forward-vs-merge-commit solid, conflict resolution sequence was missing add/commit steps first pass (said "edit then push"), corrected to full edit→add→commit→push. **Text processing/scripting** — `uniq` adjacency solid, `sed` command was missing `-i` (would've only printed to screen, not actually edited the file — a real functional bug, not just style), `-x` test was misunderstood as a content check before correcting to "checks the execute permission bit," and the shebang mechanism explanation flat out failed on first pass — user gave direct, valuable feedback that the explanation was too jargon-heavy/abstract ("I understood not a single word") and asked for concrete examples over abstract description; rebuilt using their own already-run scripts as the concrete anchor and a simple "what if it said python instead" check — landed clean on retry. **Add to how-to-teach-this-user notes**: when an abstract mechanism explanation fails, don't just repeat it — rebuild it around something they've already personally run, and check with a concrete "what if X" question, not a restate-the-definition question. **Azure/AZ-900** — tenant/sub/RG, ARM-works-regardless-of-tool, and token refresh all solid cold. Regional-vs-global reasoning was thin ("manage costs") — corrected to the real drivers (latency, redundancy/DR, data residency/compliance). NSG port scenario was a genuine misconception (thought "bill exploding" was the risk of an open port) — corrected to the real mechanism and real risk (network-level access ≠ authentication; open NSG rules are a real brute-force attack surface) and landed correctly on restate.

2026-09-02b | Started first real Azure CLI compute work: `az vm create` (rg-vm-learning). Good "why would an engineer type this by hand" question — answered honestly (shell history, --help, saved scripts, and that real production work uses Terraform for exactly this reason, which primes the upcoming curriculum step). Hit a genuine real-world error: `Standard_B1s` unavailable due to capacity restrictions in `northeurope`, then also `Standard_B2s` in `northeurope`, then `Standard_B1s` again in `eastus` — three failed attempts, real SKU capacity issue, not a mistake on our end. Good rep at reading a noisy CLI error traceback and finding the actual `Message:` line buried in Python plumbing, though it took three refusals ("do it") before engaging — flagged as a repeat of the "answer with work, not about the work" pattern from CLAUDE.md's explicit rule, worth watching for again. Ran out of time before resolving — next session: run `az vm list-skus --location eastus --size Standard_B --output table` to find an actually-available size instead of guessing, then retry `az vm create`. No VM currently exists (all attempts failed at creation) — `rg-vm-learning` resource group exists but is empty, confirm and clean up if abandoning this approach.

2026-09-02c | Found real available sizes via `az vm list-skus` (ARM B-series, restrictions: None), tried `Standard_B2pts_v2` in eastus — hit a **different, more fundamental** blocker: `QuotaExceeded`, `Current Limit: 0` for that whole VM family on this `Basic` subscription. This isn't a size-picking problem — the subscription has zero compute quota for this family entirely, common on free/basic tiers to prevent surprise billing. Next session: run `az vm list-usage --location eastus --output table` to find any VM family with non-zero quota already available, rather than guessing sizes further; if none exist, the real fallback is submitting an actual quota increase request (legitimate real task, but has real Microsoft approval turnaround, won't resolve same-session). Still no VM exists, `rg-vm-learning` still empty — no cost currently accruing.

2026-09-03 | Resumed VM quota investigation. `az vm list-usage` initially returned `[]` empty — root cause was `Microsoft.Compute` provider unregistered (same class of bug as the original Storage saga weeks back; user correctly recalled the diagnostic pattern themselves when prompted). After registering, real usage data showed several families with `Limit: 10` (DSv3, D, DS, BS, A0-A7, F, FSv2). Tried `Standard_D2s_v3` in eastus, then northeurope, then `Standard_A1_v2` in westus2 — **all three failed on `SkuNotAvailable` (capacity), not quota**. Six total failed VM creation attempts across 3 regions and multiple families/sizes today. Real conclusion, not guesswork: this `Basic` subscription is very likely capacity-deprioritized broadly, not unlucky on any single combo — flagged honestly to user rather than continuing to guess. Decision: pause live VM creation, pivot to NSG/networking concepts that don't need compute capacity. Real hands-on NSG lesson followed: created an actual NSG (`test-nsg` in `rg-vm-learning`, free resource, no capacity issues), added a real `allow-ssh` rule at priority 100, correctly predicted it beats the default deny-all (priority 65500) before running. `Microsoft.Network` auto-registered itself on first use (contrast with Storage/Compute needing manual registration — worth remembering providers vary in this). One real miss on priority ordering (said "priority 100 wins" over a hypothetical priority-90 deny rule — backwards, lower number wins) — corrected and restated correctly on retry. **Still owed**: actual working VM once capacity/quota is resolved (via support request or time), and attaching this NSG to it.

2026-09-03b | Continued into VNet/subnet, real hands-on: created `test-vnet` (10.0.0.0/16) with `test-subnet` (10.0.1.0/24), then attached `test-nsg` to the subnet — confirmed via `networkSecurityGroup` field in the subnet's own output. Correctly reasoned that subnet-level NSG attachment automatically protects anything placed inside it, no separate VM-level attachment needed. **Real pushback moment, worth remembering**: user directly said copy-pasting CLI commands wasn't building retention and asked for the actual "why is it written this way" reasoning instead of "look it up" — responded by breaking down the actual repeating skeleton every `az` command in this course has followed (`az <category> <resource-type> <action> -g <rg> -n <name> [specifics]`), reframing from "memorize strings" to "recognize one repeating shape." Also gave an honest (not just reassuring) answer on whether real engineers know these commands cold: yes, for the core/frequent ones through real repetition — that's a fair bar, not a cop-out. **New structural change, user-initiated**: going forward, 2 days off/week, user-announced, used for the hardest possible test instead of new material — saved to memory (`feedback_test_cadence`). Networking phase (VNet/subnet/NSG) now has a real, solid foundation — actual VM attachment still pending capacity resolution.

2026-09-03c | Final 20-min quiz on NSG priority ordering — the real trap version. "Broad deny at priority 100, specific allow at 200" scenario: first answer got it backwards (thought the specific office-IP rule would still get through), corrected — broad deny evaluated first catches everyone including the office IP, since NSGs go strictly by priority number, never by rule specificity. This is a genuine, common real-world misconfiguration trap (assuming "more specific" beats "more general," which is true in some other systems but not NSGs) — worth remembering as a real interview/job-relevant gotcha, not just a drill answer. User then correctly reasoned through the fix themselves unprompted (put the specific allow at the lower/earlier priority number instead), and correctly generalized the rule at the end: priority always wins regardless of specificity. Closes a strong, real day — VM capacity wall didn't block real learning, pivoted into genuine NSG/VNet/subnet hands-on work instead.

2026-09-04 | 20-min day. Added a real `.gitignore` (`*.log`, `*.tmp`) — closes a genuine long-standing Git gap and directly ties back to the old `access.log` clutter incident. Verified it actually works (created a `.log` file, confirmed `git status` never saw it), not just assumed. Small but legitimate, real push, not a hollow one.

2026-09-04b | Extended `.gitignore` with Terraform-specific entries (`.terraform/`, `*.tfstate`, `*.tfstate.backup`) ahead of that curriculum phase — real forward-looking prep, not busywork, since state files are a genuine real-world secret-leak risk if committed.

2026-09-05 | 10-min day. Found and fixed real leftover clutter in README.md — old branch/merge-lesson test lines ("branch test", "line from main directly", etc.) had sat there uncleaned for weeks. Replaced with an actual project description. Small but real, portfolio-relevant fix. User has an off-day tomorrow — per the new cadence, that means the hardest possible test, not new material.

2026-09-05b | Added `.env` to `.gitignore` — the single most critical entry to have, since that's where secrets/API keys typically live. Directly ties to the security spine (least privilege, no stored secrets) from CLAUDE.md.

2026-09-06 | First official off-day, hardest-test format (user-initiated cadence). Wave 1 (6 combined-concept scenarios): chmod-000-and-lost-ownership fix, NSG specific-deny-vs-broad-allow, tenant/sub/RG deletion scope all correct or landed after one correction. Real miss: rebuilding the `cut|sort|uniq -c|sort -rn` "most common value" pipeline from scratch — took 3 attempts (tried sed syntax, then had to be walked from "what is a field" back up to the full command) despite having built this exact pattern before; flagged as the one item needing the most repeated real use, not more explanation. Wave 2 (6 more): correctly reasoned through a genuinely harder branch-divergence trap (branch B silently diverges from `main` after branch A fast-forward-merges, even with no direct commit to `main`) once given a visual commit-graph walkthrough — real understanding, not lucky. NSG source-vs-port reasoning corrected cleanly. Token-theft-is-the-real-risk (not vague "risk") landed after one nudge. `chmod -R` factual error (said "R = read") corrected on first try. New script test flag `-s` (non-empty check) was fairly new material, not previously taught explicitly — scaffolded and landed. Overall: noticeably fewer repeat-failures than early sessions; the cut/sort/uniq pipeline remains the one genuine standing weak spot needing more real reps, not more teaching. Mid-test, added two more small real repo fixes: `.vscode/`/`Thumbs.db` to `.gitignore`, and a real MIT `LICENSE` file — both legitimate, both pushed.

Wave 3 (6 more): export-scope-across-terminals corrected (exported var only reaches child processes of that shell, not sibling terminals — real misconception, not just wording). `nohup`/SIGHUP taught as genuinely new material (background `&` jobs die on terminal close unless protected). `git origin`-after-GitHub-rename corrected (origin stays on old URL, GitHub redirects old→new, doesn't auto-update locally). `sed` without `-i` — said "idk" despite this being explicitly taught/corrected once before weeks ago, then got it right when pushed to actually answer instead of defaulting to idk. Static-vs-dynamic cost/no-per-user-logic reasoning and same-RG-name-different-subscriptions both correct cold.

Wave 4 (6 more): `bash script.sh` sidestepping the execute bit (reads via bash instead of direct kernel execution) — wrong first pass ("nothing works"), corrected, restated correctly. Non-overlapping-line merge correct cold. `Standard_LRS` cost tradeoff — fair, untaught, correct instinct (cost) without prior teaching. Git mode `644`→`755` meaning — wrong first pass ("name must be changed"), corrected by tying back to the very-first-session lesson on git mode tracking, restated correctly. NSG single-rule scope — right conclusion, needed the nuance that the trusted IP is only trusted for that ONE port, not the whole VM. `grep -c` — not confidently known despite being visible in `grep --help` output from week 1, taught properly now.

**End-of-day self-assessment, requested by user, given straight**: 24 hard combined-concept questions across 4 waves. 6/24 correct cold on first try (25%). ~6/24 were genuinely new/untaught material (fair, not counted as failures). ~12/24 wrong-then-corrected-then-restated-correctly (the dominant pattern — importantly, almost all needed only ONE correction pass, not the 3-5 pass grinds from early sessions). Only 2 items needed 2+ passes (cut/sort/uniq pipeline, branch-divergence trap). Rated: cold-execution-under-pressure ~40/100 (the metric that actually matters for "can do a real task unaided"), reasoning-once-given-the-missing-piece ~80/100 (the metric that shows the underlying skill is real), composite ~55/100. The real trajectory signal: correction now typically takes one pass instead of five — that's the mechanism that was actually broken weeks ago, and it's now visibly working. What's still missing is turning "correct after one correction" into "correct cold," which is exactly what the off-day hard-test cadence is designed to close over time, not new material.

2026-09-07 | 3hr day, fresh-wording retest of yesterday's exact weak spots. NSG-scope-nuance, export-scope-across-terminals, and sed-without-`-i` all retained genuinely well overnight — real progress, first time these specific items held up on a fresh day. `cut/sort/uniq` pipeline and branch-divergence-trap both FAILED AGAIN on first attempt (now 2nd failure each on the exact same traps) — confirms these are real standing weak spots, not one-off misses. Branch divergence landed on restate once re-explained. `cut/sort/uniq` got real remediation this time: switched from "explain again" to genuine ground-up construction — built `sort|uniq -c|sort -rn` piece by piece on real data, including catching my own bad first example (data that coincidentally looked the same sorted-alphabetically vs sorted-by-count, proving nothing) and fixing it with data where the two orders clearly disagree (zebra highest count, but last alphabetically) — user could then see `sort -rn`'s actual effect for the first time rather than just being told about it. Successfully derived the full pipeline from scratch afterward, unprompted, correctly recognizing `cut` wasn't needed for one-word-per-line input (adapting the pattern to context, not just pattern-matching the exact same command). `grep -c` needed one more concrete walkthrough but landed. **Process note**: for `cut/sort/uniq` specifically, ground-up construction with deliberately contrastive example data worked where repeated explanation had failed twice — use this approach for any other item that fails a 2nd time.

2026-09-07b | Same session, continued. Taught `git stash`/`stash pop` — clean real rep, correctly predicted the file would show original content mid-stash. Clarified `git checkout <branch>` (switch) vs `git checkout -- <file>` (discard changes, same command name doing two different jobs — a real git wart) and the newer `git restore` alternative. **Major discussion, worth remembering**: user hit real frustration ("I don't even know bash," asked whether to learn bash separately via YouTube for speed) while working through Identity/RBAC — root cause diagnosed as conflating "unfamiliar `az` CLI syntax" with "don't know bash," which the actual log data doesn't support (extensive real bash coverage, improving retention). Gave an honest, data-grounded recommendation against switching to passive video learning, citing this course's own evidence that active construction/"why"-interrogation (the `cut/sort/uniq` fix, the shebang pushback) is what's actually been working, vs. passive re-explanation needing 3-5 sessions to land. Committed to breaking down new `az` command structure more explicitly going forward rather than assuming instant recognition of new commands. **Watch for this pattern again** — mid-frustration is exactly when this user is at risk of abandoning a working method for a feeling-faster one; the fix each time should be a real, evidence-based answer, not just reassurance. Then started Identity/RBAC (who/what-role/scope model, inheritance downward through tenant→sub→RG→resource) — real hands-on: `az role assignment list` empty at resource-group scope (direct-only by default) vs populated at subscription scope, correctly reasoned through why inheritance still applies despite the empty direct-scope query.

2026-09-08 | 1hr day, post-laptop-restart. Real troubleshooting: fresh terminal dropped into `~` not the repo (needed `cd` back), `az login` subscription prompt rejected typed selections ("1", account name) until pressing Enter alone on the blank default — worth remembering for next re-auth. Taught service principals vs managed identities (the concrete real example of "managed identities over stored secrets" from the security spine) — real hands-on: created an actual user-assigned managed identity (`test-identity`), no VM needed, confirmed no secret/password field anywhere in its output, just `clientId`/`principalId`. Lock-in on connecting to yesterday's RBAC: first answer named a role ("owner or reader") instead of the mechanism; second attempt ("az role reader create") got the pattern backwards — corrected by mapping directly onto yesterday's `az role assignment list`, same category/resource, swap the action to `create`. Full flag-level hands-on (`--assignee`/`--role`/`--scope`) still owed next session.

2026-09-08b | Extra hour, completed the owed hands-on. Correctly predicted `az role assignment list --assignee <id>` would be empty before creating anything. Ran real `az role assignment create` (Reader role, `test-identity`, scoped to `rg-vm-learning`) — succeeded with a full real assignment object returned. Hit a genuine Azure CLI quirk: `--assignee`-based list queries stayed empty even after retrying (known unreliable resolution for managed-identity/service-principal-type assignees), while `--scope`-based list correctly showed the real assignment. Real, useful lesson banked: when `--assignee` search comes up empty but you have proof something exists, query by `--scope` instead as the reliable fallback. Identity/RBAC now has a genuinely complete first pass: role assignment model, managed identity creation, and real role-grant + verification, including troubleshooting a real tooling quirk rather than a conceptual gap.

2026-09-09 | 6hr day. Recall (4 spaced items): chown/sudo command syntax needed the actual command spelled out (had the reasoning right, skipped the command itself), NSG priority and branch-divergence-trap both correct cold (real retention — divergence trap in particular held up two sessions after needing a drawn graph), apt install-vs-upgrade mixup recurred once more then self-corrected. User asked for a full honest progress review (2 reference start dates given — July 31 stated vs Aug 18 actual diagnostic date, 18-day untracked gap flagged honestly since no data exists for it) and a structured topic roadmap to pick through one at a time going forward: Storage (in progress) → Compute/VM blocker resolution → Networking remainder (DNS/private endpoints/LB/VPN) → Identity remainder (app registrations/conditional access) → Monitoring+cost mgmt → Git remainder (rebase/tags) → Terraform → Docker → CI/CD → Kubernetes (literacy only). Started Storage depth: connected the RBAC/managed-identity lesson directly to the `--auth-mode login` warning seen weeks ago on the original deploy.sh run — real full-circle moment, waiting on user to confirm continuing Storage vs picking a different topic from the list.

2026-09-09b | Continued Storage depth for real, hands-on, with an actual storage account (`teststorage963957`, torn down at the end — cost hygiene maintained). **Major standing preference change**: user explicitly asked for maximum difficulty on every question from now on, not just off-days — stated it's about exposing their own tendency to predict answers via English-word pattern-matching rather than real reasoning, explicitly not an ego/dignity concern; saved to `feedback_quiz_style` as a standing escalation. Real gotcha hit and resolved: `az storage container create --auth-mode login` succeeded (management-plane action, covered by subscription `Owner`), but `az storage blob upload`/`list` failed with a genuine authorization error — **management plane vs data plane** is a real, deliberate Azure split; even subscription `Owner` doesn't automatically grant blob data-plane access, by design. Fixed with a third real `az role assignment create` (this time granting themselves `Storage Blob Data Contributor`, after `test-identity` yesterday) — same command pattern, third repetition, genuinely becoming reflexive. Scaffolded a hard combined question (why did container-create succeed vs would `test-identity`'s Reader-only role allow blob upload) down into atomic steps after an initial "I don't understand anything" — landed correctly once broken into read-only-vs-write-action pieces, then reconstructed the full mechanism themselves. Full real round-trip achieved: created storage account → hit data-plane authorization wall → diagnosed via error message → fixed via role assignment → uploaded and listed a blob via pure RBAC, zero account keys or secrets anywhere — the complete "managed identity/RBAC over stored secrets" pattern from the security spine, working end to end for the first time.

2026-09-10 | 8hr day, recall opened at max difficulty (standing preference now, not just off-days). 4 hard combined questions: NSG-vs-OS-firewall defense-in-depth correct cold. `git stash pop` conflict misconception corrected ("nothing staged means no conflict possible" is false — stash pop reapplies against *current* state, which can differ from when stashed). `Contributor`-vs-data-plane needed the full mechanism re-walked (management-plane roles never auto-grant data-plane access, regardless of how powerful — proven by yesterday's own Owner experience) before landing. Orphaned-role-assignment concept needed the specific word drawn out but substance was right early. **Real meta-moment**: user asked directly whether "Azure CLI next" was even the right curriculum step given how much unfamiliar Azure territory remains (VM, networking, storage, identity, monitoring, IaC, databases, governance) — clarified that Azure CLI was never a standalone finished phase, it's the tool used to go deep on every domain after it, and mapped their listed domains onto the existing roadmap to show the sequence is intact, not scattered. Gave full detailed explanations of blob storage terminology and Owner/Contributor/Reader distinctions (real gaps, not taught this explicitly before) after being asked directly rather than guessing. Then re-tested all 3 corrected concepts with freshly-worded variants applied to a NEW service (Key Vault instead of Storage) and a NEW identity type (system-assigned instead of user-assigned) — all 3 landed correctly, real generalization, not memorized wording. Good day for depth despite starting with several corrections. Short day overall — only ~1hr of the planned 8 actually used; real content in that hour, but flagged honestly as a pacing shortfall, not glossed over.

**NEXT SESSION — RESUME HERE:**
- Storage depth is functionally complete (management vs data plane, RBAC-based blob access, real round-trip). Not yet touched: storage redundancy tiers (LRS/ZRS/GRS) beyond the one-line SKU mention weeks ago, blob access tiers (Hot/Cool/Archive).
- Compute/VM blocker still unresolved — `rg-vm-learning` still has no VM. Options on the table: retry `az vm list-usage` for a fresh quota check, or file a real Azure quota-increase support request.
- Networking remainder: routing, load balancers, DNS, private endpoints — NSG/VNet already solid, this is the missing rest of that phase.
- Identity remainder: app registrations, conditional access — core RBAC/managed identity already solid.
- Git remainder: rebase, tags — branches/merge/conflict/stash/diff already solid.
- Monitoring + cost management: zero coverage, next full phase after Storage/Networking/Identity wrap.
- Standing rule now in effect every session: max-difficulty questions by default (see `feedback_quiz_style` memory), not just off-days.

2026-09-11 | Major method-fix session. User self-diagnosed that Azure CLI commands (unlike Linux/Git) have mostly been copy-pasted rather than built, directly explaining the repeated failures reconstructing `az role assignment create` and `--scope` resource-ID paths from scratch. Confirmed by direct test: asked to state `--scope`'s 5 sub-pieces separately (sub id / RG / provider namespace / resource type / resource name) — merged provider-namespace and resource-type together, and openly said restating the correction back "is just copy-pasting, not retention." **This is correct and important — logged as a real methodology point, not just a content gap**: going forward, immediate restatement after a correction is NOT treated as proof of retention, only as a first checkpoint; the actual test is a cold, unprompted retest on a separate day. Standing fix saved to `feedback_quiz_style` memory: Azure CLI must be built piece-by-piece by the user (same method that worked for shell scripts and cut/sort/uniq), never handed over as a complete command to copy. **New explicit weak-list item, unconfirmed, needs real cold retest on a later day**: `Microsoft.Storage`-style provider namespace vs `storageAccounts`-style resource type as two separate concepts within a resource ID, and constructing `az role assignment create` fully from scratch without a given skeleton.
2026-08-26 | 4hr day. Recall on newest/shakiest items. `~` vs `pwd` (5th session testing this) — wrong again on first pass ("~ takes us to owner/user of directory"), switched tactics (had them literally type `echo "~ always equals /home/bunny..."` instead of just re-explaining) — finally landed clean both in the abstract and applied to a new location (`/etc`). Worth remembering this tactic (make them type the fact, not just read it) if other items keep failing verbal-only correction. ARM and man/--help both correct cold this time — holding. Regional-vs-global "why" correct (minor phrasing slip: said Entra ID is "part of the Azure portal," corrected — portal is just the UI, not what Entra ID is). `apt update` vs `upgrade` needed a long real dig — user pushed back asking genuine "why" questions (why is network slow, what is metadata, catalog of what) rather than just accepting corrections; walked through network latency vs local reads, metadata as package labels vs actual package contents, using their own `tree` install as the concrete example — ended correct and reasoned-through, not memorized, but took most of the session. Good sign: this user learns better when allowed to interrogate the "why" rather than just being told to restate — lean into that pattern instead of just repeating corrections. Git `origin` FAILED TWICE even after re-teaching (first: "read-only snapshot," second: "digital location of the local main" — both wrong, keeps inventing a relationship to the local branch that isn't there). Out of time to resolve in the drill itself, but user circled back unprompted after session close and restated it correctly ("nickname for the remote URL, used instead of the full URL") — good sign of self-correction, but this was right after being told the answer, not a fresh cold pull, so still flagged for a real unannounced cold retest next session before trusting it. Session ran short (30 min left after recall alone) — no new-concept or project block today, recall/depth work ran the full available time.